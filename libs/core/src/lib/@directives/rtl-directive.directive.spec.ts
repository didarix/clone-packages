import { RtlDirective } from './rtl.directive';

describe('RtlDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RtlDirective();
    expect(directive).toBeTruthy();
  });
});
